import React from 'react';
import ReactDOM from 'react-dom/client';
import { IntelligentDashboardApp } from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <IntelligentDashboardApp />
  </React.StrictMode>,
);
